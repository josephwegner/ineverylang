<%
  json = Hash[solutions.map { |solution| 
    solution.delete(:filename)
    solution.delete(:smallFooter)
    solution.delete(:index)

    [solution[:language], solution] 
  }].to_json
%>
<%= "ddg_spice_in_every_lang(#{json});" %>
